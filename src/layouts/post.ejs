<% locals.title = post.title;
  if (post.description) {
    locals.description = post.description;
  }
%>
<% include _header %>
<div class="single">
	<div class="grad-overlay"></div>
<% include _post %>
</div>
<div class="post-body markdown">

	<%= post.genre %> <%- post.dated %>
	<%- post.content %>

</div>
<% include _footer %>
